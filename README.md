# CO3098_LAB2
## Vivado Simulation Setup

Before running the simulation:

- Go to:
  ```
  Flow → Settings → Simulation
  ```

- Change:
  ```
  xsim.simulate.runtime = all
  ```

- Click **OK** and re-launch simulation.
